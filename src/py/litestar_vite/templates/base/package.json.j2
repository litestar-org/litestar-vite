{
  "name": "{{ project_name }}",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "{% if use_typescript %}{% if framework in ['vue', 'vue-inertia'] %}vue-tsc -b{% else %}tsc{% endif %} && {% endif %}vite build",
    "preview": "vite preview",
    "serve": "vite preview",
    "watch": "npm run serve"{% if generate_client %},
    "generate-types": "openapi-ts"{% endif %}
  },
  "dependencies": {
{%- for dep in dependencies %}
    "{{ dep }}": "latest"{% if not loop.last or generate_zod %},{% endif %}
{%- endfor %}
{%- if generate_zod %}
    "zod": "^3.24.0"
{%- endif %}
  },
  "devDependencies": {
{%- for dep in dev_dependencies %}
    "{{ dep }}": "latest",
{%- endfor %}
{%- if generate_client %}
    "@hey-api/openapi-ts": "latest",
{%- endif %}
    "litestar-vite-plugin": "latest",
    "vite": "^6.0.0"
  }
}
