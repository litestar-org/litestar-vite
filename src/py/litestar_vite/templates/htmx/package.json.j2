{
  "name": "{{ project_name }}",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "vite preview"
  },
  "dependencies": {
    "htmx.org": "{{ package_version('htmx.org') }}"
  },
  "devDependencies": {
{% if use_tailwind %}    "@tailwindcss/vite": "{{ package_version('@tailwindcss/vite') }}",
{% endif %}    "litestar-vite-plugin": "{{ package_version('litestar-vite-plugin') }}",
{% if use_tailwind %}    "tailwindcss": "{{ package_version('tailwindcss') }}",
{% endif %}    "typescript": "{{ package_version('typescript') }}",
    "vite": "{{ package_version('vite') }}"
  }
}
