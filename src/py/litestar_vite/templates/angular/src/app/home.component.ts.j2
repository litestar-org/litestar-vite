import { Component, signal } from "@angular/core";

@Component({
  selector: "app-home",
  standalone: true,
  template: `
    <main class="mx-auto max-w-5xl space-y-6 px-4 py-10">
      <header class="space-y-2">
        <p class="font-semibold text-[#edb641] text-sm uppercase tracking-[0.14em]">Litestar · Vite</p>
        <h1 class="font-semibold text-3xl text-[#202235]">{{ project_name }}</h1>
        <p class="max-w-3xl text-slate-600">Angular {{ "{{" }} angularVersion {{ "}}" }} with zoneless signals + Vite + Litestar.</p>
      </header>

      <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-lg shadow-slate-200/40">
        <h2 class="font-semibold text-[#202235] text-xl mb-4">Getting Started</h2>
        <ul class="space-y-2 text-slate-600">
          <li class="flex items-start gap-2">
            <span class="text-emerald-500">✓</span>
            <span>Assets served from <code class="bg-slate-100 px-2 py-0.5 rounded text-sm">{{ asset_url }}</code></span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-emerald-500">✓</span>
            <span>Zoneless change detection with signals</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-emerald-500">✓</span>
            <span>Type generation enabled (see <code class="bg-slate-100 px-2 py-0.5 rounded text-sm">src/generated</code>)</span>
          </li>
        </ul>
        <div class="mt-4 pt-4 border-t border-slate-100">
          <p class="text-sm text-slate-500">
            Counter: {{ "{{" }} count() {{ "}}" }}
            <button
              class="ml-2 px-3 py-1 bg-slate-900 text-white rounded-lg text-sm hover:bg-slate-700 transition"
              (click)="increment()"
            >
              +1
            </button>
          </p>
        </div>
      </section>
    </main>
  `,
})
export class HomeComponent {
  count = signal(0);
  angularVersion = "21";

  increment() {
    this.count.update((n) => n + 1);
  }
}
