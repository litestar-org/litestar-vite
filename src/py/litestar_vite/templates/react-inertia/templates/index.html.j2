<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ project_name }}</title>
    {{ "{{ vite_hmr() }}" }}
    {{ "{{ vite_asset('" + resource_dir + "/main.tsx') }}" }}
  </head>
  <body>
    {{ "{{ inertia|safe }}" }}
  </body>
</html>
